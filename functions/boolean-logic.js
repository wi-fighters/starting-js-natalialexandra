// boolean logic

// true
if(true)    // works

// false
if(false)   // it doesn't work / run

// logical and
if(false && true) // evaluate to false

// logical false
if(false || true) // this evaluate to true

// examples
if ("black" === "bright" || "black" === "black") // first is false and second is true, evaluate to true
if ("black" === "bright" && "black" === "black") // evaluate to false

